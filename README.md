
  # Tournament Management Platform

  This is a code bundle for Tournament Management Platform. The original project is available at https://www.figma.com/design/z2aNoCOw2RnPPwscp7OQuJ/Tournament-Management-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  